# 作業メモ — 株式会社電総 コーポレートサイト

> これまでの経緯・判断・未完了タスクをまとめたもの。
> 会話が途切れても、このファイルを読めば作業を再開できる状態を目指している。
> 最終更新：2026-08-16（/service/ 参照デザイン準拠）

---

## 1. 前提

| 項目 | 内容 |
|---|---|
| クライアント | 株式会社電総（Densou Co., Ltd.） |
| 事業 | 電気設備・空調・換気/ダクト・弱電/通信・消防/防災 |
| 体制 | 代表者1名。電気工事歴30年。一人目の社員を募集中 |
| 所在地 | 神奈川県平塚市片岡879-5 |
| 電話 | 090-3340-3130 |
| 対応エリア | 神奈川・東京・山梨・千葉・静岡（1都4県） |
| 技術構成 | ビルド無しの静的HTML/CSS/JS。GitHub Actions → GitHub Pages |
| リポジトリ | `DendoJapan22/KabusikigaisyaDensou_Web` |
| 公開ブランチ | `claude/website-construction-qsimq3`（GitHub Pages はここから deploy） |
| 公開の運用 | 作業ブランチにコミット後、qsimq3 を fast-forward して push（ユーザー承認済み・以後は都度確認不要） |

PRは明示的な依頼があるまで作らない。

---

## 2. 守り続けている制約

### AI生成画像を使ってはいけない箇所

1. **代表者の顔写真**（`/company/` `/recruit/`）— 実在の人物。生成した顔を載せるのは経歴の捏造にあたる
2. **施工事例として提示するすべての写真**（`/service/` の工事メニューおよびトップの「対応可能な工事内容」）— 自社実績としての虚偽表示になる。サイトに「実際の現場から」と明記している

加えて実務上、AIは電気工事の細部（盤内のバー配置、渡り線、端子処理、ケーブルの色分け、保護具の着用）をほぼ確実に誤る。一般の閲覧者は気づかないが、**このサイトの最重要ターゲットである元請・同業者は一目で見抜く。**

### 生成画像に共通して入れてはいけない要素

- 人物・顔・手（すべて）
- 配線・端子台・分電盤内部のクローズアップ
- 文字・ロゴ・看板・ナンバープレート
- 過剰な彩度、HDR、レンズフレア、ネオン、サイバーパンク調

### その他

- `docs/contents.md` / `docs/design.md` はクライアント支給の原本。上書きしない
  （現行サイトの仕様は `docs/site-contents.md` / `docs/site-design.md` に別途記述）
- コミットメッセージ・PR・コードにモデル識別子を書かない

---

## 3. トップページの構成

```
ヒーロー（左：濃紺べた／右：夕暮れ→夜のタイムラプス動画）
└ nightzone ─ 濃紺 #08192B・上端は中央が持ち上がる弧
   ├ 事業内容      パワーライン（蛇行する電線＋スクロール連動）
   ├ 選ばれる理由 ＋ 電球型の穴
   ├ 対応可能な工事内容（実写3枚の非対称グリッド）
   └ 対応可能な工事内容で終了
対応エリア（氷青。支給デザイン準拠：地図＋下部に薄い街並み）
お客様に合わせたご案内（白）
採用バナー（ミスト）
会社概要（白）
└ endzone ─ CTA＋フッターが一体。ライトの画像が背景
```

キャッシュ対策：`index.html` は `style.css?v=18`、下層ページは `?v=10`。
GitHub Pages はCSSを10分キャッシュするため、CSS変更時は必ずバンプする。

モバイルのハンバーガーは、トップページでは `position: fixed` ＋ 紺の面
（スクロールしてもヘッダーごと流れない）。開いている間は面を消して紺の×だけにする。

---

## 4. 主要な実装

### 4-1. ヒーロー

12.4秒のタイムラプス動画（`hero-dusk.mp4` 1.75MB / `.webm` 1.26MB）。
`autoplay muted playsinline preload="auto"`、**ループさせず最後のコマで止める**。
`prefers-reduced-motion` と `navigator.connection.saveData` では動画を外してポスター画像に。

縦書き明朝のコピー「住まいの電気を、／三十年つくってきた。」（数字は漢数字）。
下に信頼バー3項目（カウントアップあり。値が2未満のものはスキップ）。

### 4-2. パワーライン（事業内容）

スクロールに合わせて電線が引かれ、電子が先行し、通過した項目の写真が左右交互に浮かび上がる。

- DOMの実測（`getBoundingClientRect`）からSVGパスを生成。角は45°で面取り
- `getTotalLength()` / `stroke-dasharray` / `stroke-dashoffset` で描画量を制御
- `getPointAtLength()` で先端の光点を配置
- 彗星の尾：同じパスを複製し、移動するダッシュ区間を作る
  （`dasharray = len + " " + (wireLen+200)`、`dashoffset = len - pos`）
- 写真は `clip-path: inset(100% 0 0 0)` → `inset(0)` で下から立ち上げる
- ラベルはタイプライター表示（420ms待ってから45ms/文字。先に `offsetWidth` を固定して幅の揺れを防ぐ）
- ノード：リング（点線・回転）＋コア＋波紋＋スタブ。通過時に `is-lit` が付く
- スクロールは passive + `requestAnimationFrame`、リサイズは150msデバウンスで再構築

### 4-3. 電球型の穴（選ばれる理由）

濃紺の面を電球のシルエットで切り抜き、向こうに灯りのともった住宅街を見せる。

**アセット**：`assets/img/bulb-hole.png`（409KB）/ `.webp`（24KB）、532×918

**生成手順**（`scratchpad/hole.py`）：

1. `denkyuu_real.png`（1024×1536）のアルファチャンネルから電球シルエットを抽出。
   bbox は `(245, 310, 777, 1228)` → 532×918。閾値110で二値化し、0.9pxぼかしてアンチエイリアス
2. 中身は `hero.jpg` の `(670, 470, 866, 808)` をクロップして拡大
   - サイトの色の基準そのものなので紺の面と喧嘩しない
   - ヒーロー動画と同じ場所・同じ日の写真なので景色が繋がる
   - 実写なので「どこの街？」の問題が出ない
   - 下限を y=808 にしているのは、それ以下にベランダの手すりが写り込むため
3. 全体を暗く（Brightness 0.70 / Contrast 1.10）
4. 上に行くほど紺 `(9,24,42)` へ沈める垂直グラデーション（地平線 y=540 基準、指数0.85）。
   穴の上部が背景と同化し、地平線の残照と家々の灯りだけが残る
5. 輝度150以上のみ抽出してブルーム。**窓の灯りだけを光らせる**
   （画像全体をブルームさせると空が白飛びする。一度これで失敗している）
6. 下端（y > 0.70H）を紺に沈めて口金を背景に溶かす。
   これをやらないと、いちばん細いところで明るい家が目立って形の先端に目が行く
7. マスクをぼかしたもの（blur 12）をLUT（`0.24 + 0.76·x^0.45`）で加工し、
   切り口の内側を暗く落として面に厚みを出す。これがないと「写真を切り抜いて貼った」に見える

**CSS**：`.lightband__glow` は「穴から漏れる光」として弱めた（不透明度 .5→.20、blur 20→26px）。
明滅アニメーション `bulbGlow` は削除（穴は明滅しない）。

**未対応**：
- 採用バナーの電球（`denkyuu-real`）はそのまま。ミストグレーの面に載っているので同じ手法が使えない
- 穴のサイズは215pxのまま。背景に穴を開ける仕掛けとしては小さいかもしれないが、
  レイアウトが変わるので勝手に触っていない

### 4-4. /service/ 事業内容（参照デザイン準拠・13メニューのカードグリッド）

クライアント支給の参照画像 `assets/img/zigyounaiyou_image.png` にかなり似せて再構築。
（その前の「職人の仕様書レイアウト」は置き換え。旧CSS `svc-open` `svc-domain` `svc-others` は
他ページで未使用の死にコードとして残置）

- **ヒーロー**：紺の斜めパネル（`clip-path: polygon`）＋ 分電盤の実写（`works/bunden`）＋
  斜めの黄ライン ＋ 縁取り数字「01」＋ 縦書き英字。コピーは参照画像の
  「電気で、暮らしとビジネスの未来を支える。」を採用、リードは事実に合わせて調整
- **白の帯**：明朝のひとこと＋数字3つ（30年/1都4県/対応領域）。丸枠の黄アイコン
- **本文**：13枚の番号付きカード（写真＋紺のキャプション：アイコン・番号・題・箇条書き）
  ＋ 右に sticky の「電総の強み」サイドバー（紺パネル・内側罫・4項目）
- カードの写真は works の実写13枚と1対1対応。参照画像の「換気扇・レンジフード」は
  実写が無いため06の箇条書きに統合し、代わりにテレビ共聴(07)と弱電設備(08)を分割
- `inazumasinboru.webp`：稲妻の円形バッジ。サイドバー下に `mix-blend-mode: multiply` ＋
  回転で透かしとして配置（**注意：バッジ内の文字は "EST. 1995"。会社設立年ではない**）
- `denkikouzisyaezzi.webp`：職人2人の線画。「その他の工事も」セルに multiply で配置
- アンカー `#denki #kucho #kanki #jakuden #bosai` はカード01/04/06/07/09に移設
  （トップのパワーラインからのリンク先）
- **罠**：`.svc2-hero::before`（紺パネル）は写真より先に描画されて下に潜る。
  `z-index` を明示（media:0 / ::before:1 / slash:2）しないとモバイルのスクリムが消える

**/price/（料金・費用の目安）もこのページに統合して廃止**（ユーザー判断・2026-08-16）。
- 具体的な金額が未確定のままで「料金表」として機能していなかったため、
  実態（個人客向けの営業内容）を `#kojin`「個人のお客様へ」セクションとして移植：
  基本メニュー表／同日対応できる追加工事8項目／見積りの流れ／「追加請求なし」の約束
- 旧URLは meta refresh で /service/#kojin へ転送（noindex・canonical は /service/）
- 全ページのナビ・フッターから「料金の目安」を削除 → **ナビは4項目**
  （事業内容・対応エリア・会社概要・採用情報）
- トップの個人向けカードは「工事と見積りのご案内を見る」→ /service/#kojin に付け替え
- sitemap.xml から /price/ を削除
- **将来、実際の料金を掲載すると決めたら独立ページとして復活させる価値がある**
  （「エアコン取付 料金 平塚」系の検索の受け皿になるため）

**/works/（施工事例）はこのページに統合して廃止**（ユーザー判断：内容がほぼ重複）。
- 旧URLには meta refresh の転送スタブ＋ canonical → /service/ ＋ noindex を残置
- 全ページのヘッダー/フッターから「施工事例」を削除。トップの works-pick と
  「対応可能な工事内容を見る」ボタンは /service/ に付け替え
- 施工事例ページにあった注記（写真は実際の現場から／社名義の事例は許可確認後に公開）は
  /service/ の見出し下に移植
- sitemap.xml から /works/ を削除

### 4-5. 対応エリア（トップ・支給デザイン準拠）

クライアント支給の `taiouerea.png`（対応エリアセクションのAIモックアップ）を基に再構築。
大型タイポグラフィ版（section--band）を置き換え、**nightzone の外**に出した。

- **地図の抽出**：`taiouerea.png` から cv2.INPAINT_TELEA（半径11）で
  見出し・チェックリスト・日本列島インセット・凡例・注記・コンパスを除去し、
  x∈[280,1536] でクロップ → `area-map.jpg/.webp`（1256×1024）。
  県名ラベル・平塚市のピン・同心円はそのまま残す
- **文字はHTMLで再構築**：SERVICE AREA / 対応エリア / リード / チェック3項目
  （丸＋チェックは CSS の ::before/::after で描画）/ 対応エリアの詳細リンク
- **下部に街並みを薄く**（ユーザー指定）：`machinami.jpg/.webp`（富士山の見える都市の俯瞰、
  1600w）を `.area2::after` で敷く。opacity .16、mask で上に向かってフェードアウト、
  background-position: center bottom
- 地図の端は radial-gradient の mask で面に溶かす
- **罠**：data-echo の JS が付ける `.is-echo-host` は inline-block。
  見出しを独立行にするには `display: block` で打ち消しが必要
- 原本 `taiouerea.png` と `ChatGPT Image 2026年8月16日 13_37_30.png`（街並みの元）は
  リポジトリに残置（未参照）
- **第2弾：`taiouerea_demo.png` 準拠に更新**。地形図ではなく半透明のフラットな県形＋
  神奈川の黄色い同心円の地図（`area-map2.jpg/.webp` 1036×860。デモから切り出し、
  左端の文字残りをインペインティングで除去）。街並みはセクション全体に透ける
  （opacity .28・上から下へのフェードマスク）。/area/ ヒーローは明朝のサブコピー＋
  本文2段落＋白いカード（斜めの黄アクセント＋ピン）＋上端が斜めに切れる白い
  3特徴バンド（走る人・地図とピン・吹き出しの線画アイコン）。旧 `area-map.*` は削除
- **/area/ ページも同じ言語で再構築**：ヒーローは `.area2 .area2--page`（h1＋パンくず＋
  チェック3項目＋地図）、続いて「都県ごとの対応」の6カード（神奈川に拠点タグ・黄枠）。
  旧・矩形SVGの概略図と card-grid は撤去。
  **罠**：`.area2--page .area2__inner` は詳細度が (0,2,0) で 899px メディアクエリの
  `.area2__inner` (0,1,0) に勝ってしまう。モバイル1カラム化はメディアクエリ内でも
  `--page` 付きで明示する

### 4-6. PDCA第1周（2026-08-16・下層ページの脱・単調化）

全ページ調査で「新デザインのページと旧ページの落差」が最大の課題と判明。改修：

- **代表者メッセージ（company/recruit）**：「PHOTO COMING SOON」のプレースホルダーを廃止。
  黄ルール＋明朝の引用リード＋2段組（誌面風）の `.msg2` に。
  代表の実写が届いたら差し込む（コンポーネントに写真スロットを足すだけ）
- **会社概要の表**：灰色ゼブラをやめ `.spec2`（上太罫＋細罫の一覧・電話は大きくAnton）
- **採用の1日の流れ**：素のリストから `.dayline`（アンバーの電線＋白抜きノード、
  最後の退勤ノードだけ黄で発光＝サイトの「灯り」モチーフ）
  **罠**：`.article ul li::before`（ダッシュ罫、0-1-4）が `.dayline li::before`（0-1-1）に
  勝つ。`.article .dayline li::before`（0-2-2）で上書きする
- **お問い合わせ**：種別カードに斜めの黄アクセント、FAQのQを紺丸バッジに
- **見出しの残像（エコー）演出を全廃**（ユーザー評「チープ」）。
  `.echo-echo { display:none }` とし、`.echo-solid` を
  opacity＋translate(.3em)＋blur(7px)→0 の「にじみから結像」に変更。
  灯りが点くイメージ。各所の echo-echo 色指定は死にコードとして残置。
  main.js は残像スパンを生成し続けるが非表示（JS未変更・低リスク優先）
- 次周候補：/service/work/ の8ステップも dayline 化、採用ページの2カラム化
- **ヘッダー画像差し替え済み（ユーザー生成のChatGPT画像）**：
  会社概要 → `company-head.jpg/.webp`（夕暮れの住宅街と灯りのついたマンション。回路図から差し替え）
  採用 → `recruit-head.jpg/.webp`（紺の工具箱とケーブル。ブランド色と一致）
  原本2枚（日付名PNG）はリポジトリに残置（未参照）

### 4-7. endzone（CTA＋フッター）

`aspect-ratio: 1920/852` ＋ `background-size: 100% 100%` で画像を切らずに全部入れる。
スクリムは上が明るく下が暗いグラデーション（均一にかけるとランプの傘が溶ける）。
モバイルは `background-size: 100% auto` ＋ `background-position: left top`、
`.cta-block` の上パディングを `calc(44.4vw + var(--s-4))` にして画像の下から始める。

---

## 5. 保留中の構想：電球ポータル

**状態：ユーザーの指示により保留。「後でまた話すかもしれない」**

### 何をしようとしていたか

スクロールで電球の中に視点が入り、街が見え、カメラが上昇して関東圏の俯瞰になり、
そこで対応エリアが文字とともに確認される、という演出。

### 作った動作プロトタイプ

https://claude.ai/code/artifact/e2f74817-8711-4766-a9c2-3546156534ad

sticky ＋ 3.4画面のスペーサー。`clip-path: circle()` で電球のフィラメント位置から穴が開き、
拡大しながらK1〜K4の4キーフレームをスケール＋クロスフェードする。中身は仮画像。
（ソースは `scratchpad/portal.tpl.html` ＋ 画像を data URI に埋める `portal.html` ビルド）

### 議論で出た結論（再開時に読むこと）

- 出口は「街 → 対応エリア」で決定していた。ただし実装するなら
  **「対応可能な工事内容」を電球より前に移す必要がある**。
  いま電球と対応エリアの間に挟まっていて、ポータルが成立しない
- **動画生成AIで1本の動画を作ってそのまま置くのは非推奨。** 理由3つ：
  1. スクロールでのシークが重い（全フレームIフレーム化か画像シーケンスが必要）
  2. 4段のカメラワークを5〜10秒尺で一発成功させるのは博打。
     ヒーロー動画でも「ズームして巻き戻る」事故が起きている
  3. **AIは関東の海岸線を確実に間違える。**
     対応エリアという事実を伝える場所で嘘の絵を出すことになる
- 推奨：生成するのは静止画のキーフレームのみ。
  最後の俯瞰カットは**SVGで1都4県の輪郭と県名をHTMLで重ねる**。
  生成画像は「夜の平野に広がる光」に留めて地理的正確さを主張しない。
  正確さはSVGが担保し、県名はテキストなので編集も読み上げもできる
- プロトタイプを作って判明したこと：**K2（ガラスの内側）は生成不要。**
  電球画像の中身をそのまま引き伸ばすほうが連続性が出る。
  生成が必要なのは K3（街の真上）と K4（上空）の2枚だけ
- 実装上の代償：モバイルで指3回ぶん固定される。スペーサーは2.5画面までに抑えたい。
  `prefers-reduced-motion` ではK1とK4だけを普通に並べる分岐が必須

---

## 6. アセットの現状

### 実写（クライアント提供・13枚）

`assets/img/works/` に配置。すべて実際の現場写真。
`entrance` / `meter-panel` / `service-panel` / `bunden` / `booster2` / `catv-booster` /
`alarm` / `outdoor-light` / `aircon-piping` / `aircon-recovery` / `outdoor-unit` /
`duct` / `shop-interior`

※明るさ・色温度がバラバラ。トーン統一は未実施。

### 主要な画像

| ファイル | 用途 |
|---|---|
| `bulb-hole.png/.webp` | 電球型の穴（532×918） |
| `denkyuu-real.png/.webp` | 採用バナーの電球 |
| `denkyuu_real.png` | 上記の原本（1024×1536、アルファが綺麗。穴のマスク元） |
| `denkyuu_toumei.png` | 透過ガラス版の原本。ポータル実装時のマスク素材候補として保管 |
| `hero.jpg` | 夕暮れの街。**このサイトの色の基準**。穴の中身の元画像 |
| `hero-poster.jpg/.webp` | 動画のポスター |
| `footer.jpg/.webp` | endzone の背景（トーンマッピング済み） |
| `circuit.*` | 回路図パターン |
| `bg-paper-*` | 方眼＋ドットの紙テクスチャ6色 |
| `og/*.jpg` | OGPカード9枚 |
| `area-map.jpg/.webp` | 対応エリアの地図（taiouerea.png からデザイン要素を除去・1256×1024） |
| `machinami.jpg/.webp` | 街並みの俯瞰写真。対応エリア下部の薄い背景 |
| `taiouerea.png` | 支給モックアップ原本（未参照・保管） |

### 動画

`assets/video/hero-dusk.mp4`（1.75MB）/ `.webm`（1.26MB）— 12.4秒

---

## 7. 検証環境（再現手順）

```bash
python3 -m http.server 8321
```

Playwright（chromium は `/opt/pw-browsers/chromium`。`playwright install` は不要）で
スクリーンショット・JSエラー捕捉・横スクロール検査。
検査幅は 320 / 390 / 768 / 1440 / 1920。

- ffmpeg は `imageio_ffmpeg.get_ffmpeg_exe()` で取得（apt は使えない）
- 通信スロットリングは CDP `Network.emulateNetworkConditions`。
  パラメータ名は `downloadThroughput` / `uploadThroughput`（`download` / `upload` ではない）
- **コントラスト測定時はテキストを非表示にしてからサンプリングする。**
  描画済みの白文字を含めると数値が跳ね上がる

パフォーマンス推移：857KB / LCP 2124ms（4G）→ 動画追加で 2.02MB / LCP 1720ms
→ バンド写真と重複ポスターの削除で 1.77MB

---

## 8. 過去にはまった罠

| 症状 | 原因 |
|---|---|
| index.html に孤児マークアップが残った | 非貪欲な正規表現 `<div ...>.*?</div>` が最初の内側 `</div>` で止まった |
| `.page-head > .wrap` が284pxに潰れた | grid item に `margin-inline: auto` → shrink-to-fit。`width: 100%` で解決 |
| page-head の写真が帯を破壊 | 高さ auto の親の中で `<img height:100%>` が自然高にフォールバック |
| フッターのランプが見えない | ①1920px以上で `background-position: left center` が切っていた ②モバイルの32%シフトで画面外 ③均一スクリムが暗い傘を溶かした |
| CTA見出しのコントラストが1.9 | スクリムを濃くするとランプが死ぬので、**元画像をトーンマッピング**（filmic rolloff, T=150 P=2.5）して解決。全ブロック5.0:1以上に |
| パワーラインの写真が見えない | `.powerline__photo` は `<span>`。`display:block` がないと `aspect-ratio` も `clip-path` も効かず高さ0 |
| ナイトゾーンの弧で横スクロール発生 | `width:140%` のはみ出し要素で作っていた。`border-radius: 50% 50% 0 0 / 110px 110px 0 0` に変更 |
| スクロール点灯が「変わったように感じない」 | ①デプロイ待ち ②CSSの10分キャッシュ ③**18%可視で発火 → 画面に入る前に完了していた**（設計ミス） |
| ヒーロー動画が「ダサい」 | 12〜14.9秒で**ズームが巻き戻る**（ビデオの巻き戻しに見える）。クロスフェードもvidstabも失敗し、12.4秒でトリム＋ループ解除で決着 |
| ポスター画像が二重ダウンロード | `poster` にjpg、`<picture>` にwebp。posterもwebpに統一 |
| 穴の空が白飛び | 画像全体にブルームをかけていた。輝度150以上のみ抽出する方式に変更 |
| git が "not a git repository" | 連鎖コマンド内の `cd` で作業ディレクトリが変わっていた |
| ハンバーガーを開くと1項目しか見えない | メニューのパネル（`.header-right`）で基本CSSの `height: 64px` を上書きし忘れ。`box-sizing: border-box` のためパディング160pxが下限になり、160pxの帯に潰れていた。`inset: 0` ＋ `height: auto` で全画面に |

---

## 9. ユーザーからの指摘の履歴

判断基準として重要なので残しておく。

- 「なんか違うな」— 空室のマンション内観をヒーロー第2画像に提案 → 賃貸内見写真に見える。提案が誤り
- 「変わったように感じないな」— スクロール点灯の発火が早すぎた
- 「確認できた。ただいまいちだな。元に戻して」— スクロール点灯を全面撤回
- 「色を間違えました」— ナイトゾーンはヒーロー/フッターと同じ紺を使う
- 「違うんだよな。スクロールするたびに項目が出てくる感じ」— パワーラインは一括表示ではなくスクロール駆動の蛇行に
- 「この画像拡大してアップするところ、ちょっとダサくない？」— ヒーロー動画のズーム巻き戻し
- 「いまいち動きが予想できないんだけど」— 文章での説明が伝わらず、動くプロトタイプを作って解決

**傾向**：見た目の不満は必ず定量的に原因を特定してから直す
（フレーム単位の追跡、輝度パーセンタイル、ページ幅の実測など）。感覚で当てにいかない。
説明が伝わらないときは、言葉を重ねるより動くものを見せる。

---

## 10. 残タスク

### 画像制作（AI生成可）

- **パワーラインの5枚**。プロンプトは提示済み：
  閉じた鋼製キャビネット／天井カセット形エアコン／スパイラルダクト／
  屋上アンテナのシルエット／自動火災報知設備のベル
- `/service/` `/service/work/` `/works/` `/price/` のページヘッド画像
- `/recruit/` `/company/` のシーン画像
- 全18スロットの一覧は `docs/site-design.md` §7。英語のスタイル指定とネガティブプロンプトは §6

### 実写が必須

- **代表者ポートレート**（`/company/` `/recruit/` は現在 `placeholder-portrait.svg`）

### 未確定の情報（クライアント確認待ち）

- 受付時間 8:00–19:00 の可否
- 郵便番号 / 建設業許可番号 / 設立年月日
- 採用の給与・休日・待遇
- お問い合わせフォームの送信先バックエンド（未接続）
- `/voice/`（お客様の声）は未公開

### 未着手の提案

- 実写13枚のトーン統一
- 施工事例の個別記事
- **Googleビジネスプロフィール登録**（サイト速度よりSEO効果が高いと伝えてある）
- フォントのセルフホスト
- 55KBのロゴの圧縮
